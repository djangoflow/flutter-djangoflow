name: djangoflow_scrollable_column
description: A Flutter package that provides the `DjangoflowScrollableColumn` widget, allowing you to create a vertically scrollable column of widgets with ease.
version: 0.2.0
homepage: https://github.com/djangoflow/flutter-djangoflow/tree/main/packages/djangoflow_scrollable_column

environment:
  sdk: ">=3.1.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
