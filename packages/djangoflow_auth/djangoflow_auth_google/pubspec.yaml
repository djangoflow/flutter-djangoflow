name: djangoflow_auth_google
description: djangoflow_auth_google is the Google authentication provider for djangoflow_auth package
version: 0.1.0+4
homepage: https://github.com/djangoflow/flutter-djangoflow/

environment:
  sdk: ">=3.0.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  djangoflow_auth: ^0.3.0+4
  freezed_annotation: ^2.4.1
  google_sign_in: ^6.2.0
  google_sign_in_platform_interface: ^2.4.3
  google_sign_in_web: ^0.12.3
  json_annotation: ^4.8.1
  mounted_state: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.7
  flutter_lints: ^3.0.1
  freezed: ^2.4.5
  json_serializable: ^6.7.1

flutter:
