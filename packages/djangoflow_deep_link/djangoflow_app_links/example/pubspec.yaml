name: example
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  djangoflow_app_links:
    path: ../
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  djangoflow_deep_link_interface:
    path: ../../djangoflow_deep_link_interface

flutter:
  uses-material-design: true
