name: Publish djangoflow_openapi
run-name: ${{ github.actor }} is publishing djangoflow_openapi on pub.dev 🚀
on:
  push:
    branches:
      - "implement/djangoflow_openapi"
    paths:
      - "packages/djangoflow_openapi/CHANGELOG.md"
jobs:
  publish_djangoflow_openapi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: "Test run"
        uses: k-paxian/dart-package-publisher@master
        with:
          skipTests: true
          force: true
          credentialJson: ${{ secrets.CREDENTIAL_JSON }}
          relativePath: packages/djangoflow_openapi
